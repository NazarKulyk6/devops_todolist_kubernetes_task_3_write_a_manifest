apiVersion: v1
kind: Pod
metadata:
  name: busybox-curl
  namespace: todoapp
  labels:
    app: busybox-curl
spec:
  containers:
    - name: busybox
      image: ikulyk404/busyboxplus:curl
      command: ["sh", "-c", "sleep 36000"]
  restartPolicy: Always

